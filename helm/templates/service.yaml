apiVersion: v1
kind: Service
metadata:
  name: {{ include ".helm-xnl.fullname" . }}
  labels:
    {{- include ".helm-xnl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include ".helm-xnl.selectorLabels" . | nindent 4 }}
